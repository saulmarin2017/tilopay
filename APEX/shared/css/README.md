# APEX — CSS (Tilopay)

Aún no hay CSS de branding. Cuando se arme, **estos archivos dirán Tilopay**. No copiar `login_gridxia.css` ni `login_ordexia.css` sin adaptarlos.

| Archivo | Nombre en APEX | Dónde |
|---------|----------------|-------|
| `login_tilopay.css` (pendiente) | `login_tilopay.css` | Página Login → CSS → Inline |
| `app_tilopay.css` (pendiente) | `app_tilopay.css` | Theme → CSS File URLs `#APP_FILES#app_tilopay.css` |

No mezclar: el CSS de login pinta el fondo; si va en el theme, todas las páginas quedan con ese fondo.
